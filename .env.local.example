# Copy this file to .env.local for local testing, and set the same
# names as Environment Variables in your Vercel project settings.

# Safe to expose in the browser (used by the app to talk to Supabase)
NEXT_PUBLIC_SUPABASE_URL=https://ovkjmaoebyspdalibltw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_2GY55GYfisUAXrXEaJ6yiA_P6tMBx_o

# SECRET — server-side only. Never put this in NEXT_PUBLIC_*, never
# commit it to GitHub. Get it from Supabase: Project Settings > API Keys
# > "secret" / "service_role" key. Only used by app/api/create-user.
SUPABASE_SERVICE_ROLE_KEY=paste-your-secret-service-role-key-here
